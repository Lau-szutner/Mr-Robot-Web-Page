// let personaje = function () {};

// carga = (nombre) => {
//   console.log(nombre);
// };

// carga(prompt('sdsf'));

const cajon = ['olla', 'manija', 'nananan'];

const ollas = new Array();

ollas.push('ola');
